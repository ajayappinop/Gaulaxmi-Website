PORT=4000
JWT_SECRET=change-this-to-a-long-random-string-in-production

# MongoDB (optional — omit to keep JSON file storage in server/data/database.json)
MONGODB_URI=mongodb://127.0.0.1:27017
MONGODB_DB_NAME=gaulaxmi

# Frontend app URLs (used for admin ↔ member links and impersonation handoff)
VITE_MEMBER_URL=http://localhost:3000/
VITE_ADMIN_URL=http://localhost:3001/
